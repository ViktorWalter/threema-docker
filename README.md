# Threema - Docker
## Docker package for running Threema Desktop client on incopatible systems.
This installs flatpack inside the image, and uses it to install Threema desktop client.
Run `install.sh` to build the image and add a .desktop file to the host for easy launching.
